# Zephyr React Starter

This example shows local package usage for Zephyr v1.

## Steps

1. Install dependencies:

```bash
pnpm add @zephyr/core @zephyr/ui-react
```

2. Generate config/styles:

```bash
zephyr init
```

3. Use components from `@zephyr/ui-react`.
