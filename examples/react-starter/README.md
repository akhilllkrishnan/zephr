# Zephr React Starter

This example shows local package usage for Zephr v1.

## Steps

1. Install dependencies:

```bash
pnpm add @zephrui/core @zephrui/ui-react
```

2. Generate config/styles:

```bash
zephr init
```

3. Use components from `@zephrui/ui-react`.
