{
  "name": "@zephrui/ai-registry",
  "version": "0.1.0",
  "description": "Machine-readable component registry and schemas for AI tools",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "schemas",
    "registry"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && mkdir -p dist/schemas dist/registry && cp schemas/registry.schema.json dist/schemas/registry.schema.json && cp src/registry/components.json dist/registry/components.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
