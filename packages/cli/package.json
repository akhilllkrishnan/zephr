{
  "name": "@zephyr/cli",
  "version": "0.1.0",
  "description": "CLI for Zephyr setup and component workflows",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "zephyr": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@zephyr/ai-registry": "workspace:*",
    "@zephyr/core": "workspace:*"
  }
}
